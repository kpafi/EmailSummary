"""LLMProvider-Protokoll (complete(system, user, *, max_tokens, temperature) -> str) und
Fehlerklassen (LLMTimeout, LLMRateLimited, LLMInvalidResponse). Bewusst ohne Tool-/Function-
Calling (I2).

Vertrag: docs/ARCHITECTURE.md §2 (LLM-Schicht). Umsetzung in WP4.
Platzhalter (WP0): noch keine Logik.
"""
